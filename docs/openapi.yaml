openapi: 3.0.3
info:
  title: Exapp API
  description: 中小企業診断士 学習管理アプリ API仕様書
  version: 0.3.0

servers:
  - url: /api
    description: Next.js proxy (→ backend)

# ルートレベルで認証を有効化（全体適用）
security:
  - bearerAuth: [ ]

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    Unauthorized:
      description: 未認証（トークン未送信または期限切れ）
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
          example: { message: 'Unauthenticated.' }

    NotFound:
      description: 指定されたリソースが見つからない
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
          example: { message: '指定されたリソースが見つかりません。' }

    UnprocessableEntity:
      description: バリデーションエラー
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
          example:
            message: 'The given data was invalid.'
            errors:
              field: [ 'フィールドは必須です。' ]

    Conflict:
      description: リソースが既に存在する、または競合が発生した
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
          example: { message: '同じ名前のリソースが既に存在します。' }

    GeminiUnavailable:
      description: Gemini APIが応答しない（busy / 地域制限）
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
          example: { message: 'Gemini is busy (503)' }

    GeminiQuotaExceeded:
      description: Gemini APIの利用上限超過
      content:
        application/json:
          schema:
            type: object
            required: [ code, message ]
            properties:
              code: { type: string, example: 'GEMINI_QUOTA_EXCEEDED' }
              message: { type: string, example: 'AIの利用上限に達しました。しばらく時間をおいてから再試行してください。' }

  schemas:
    StopwatchState:
      type: object
      required: [ isRunning, elapsedSeconds ]
      properties:
        isRunning:
          type: boolean
          description: 現在計測中かどうか
        elapsedSeconds:
          type: integer
          description: |
            累積秒数。`isRunning = true` の場合は `保存済み秒数 + (現在時刻 - started_at)` を加算済みの値を返す。

    UserProfile:
      type: object
      properties:
        nickname: { type: string, nullable: true }
        occupation: { type: string, nullable: true }
        goal: { type: string, nullable: true }
        weakAreas: { type: string, nullable: true }
        strongAreas: { type: string, nullable: true }
        interests: { type: string, nullable: true }
        geminiTokenSet: { type: boolean, description: '個別Geminiトークンが登録済みかどうか（トークン本体は返さない）' }

    UserProfileInput:
      type: object
      properties:
        nickname: { type: string, nullable: true, maxLength: 50 }
        occupation: { type: string, nullable: true, maxLength: 100 }
        goal: { type: string, nullable: true, maxLength: 500 }
        weakAreas: { type: string, nullable: true, maxLength: 500, description: '苦手科目（カンマ区切り等、自由記述）' }
        strongAreas: { type: string, nullable: true, maxLength: 500, description: '得意科目（カンマ区切り等、自由記述）' }
        interests: { type: string, nullable: true, maxLength: 500, description: '趣味・嗜好' }
        geminiToken: { type: string, nullable: true, maxLength: 200, description: 'ユーザー個別のGemini APIキー（保存時に暗号化）' }

    AiAdviceMode:
      type: string
      enum: [ ANALYSIS, INSPIRATION, ANALOGY, WARNING ]
      description: |
        AIアドバイスのモード。
        - ANALYSIS: 目標達成率と時間ブロック配置を論理的に分析
        - INSPIRATION: 客観的な事実で学習努力を称える
        - ANALOGY: 試験概念を趣味に例えて解説
        - WARNING: 危機感を煽る冷徹な鬼コーチ

    TimeSlot:
      type: string
      enum: [ morning, lunch, night ]
    Subject:
      type: string
      description: ユーザーが定義した科目名
    Material:
      type: string
      description: ユーザーが定義した教材名

    AlertStatusItem:
      type: object
      required: [ subject, lastDate, recentMinutes, settings ]
      properties:
        subject:
          type: string
          description: '科目名'
        lastDate:
          type: string
          format: date
          nullable: true
          description: '最終学習日。未学習の場合は null'
        recentMinutes:
          type: integer
          description: '直近 settings.minutesThresholdDays 日間の学習時間（分）'
        settings:
          $ref: '#/components/schemas/SubjectAlertSettings'
          description: 'この科目のアラート設定。未設定の場合はデフォルト値'

    SubjectAlertSettings:
      type: object
      required: [ touchAlertEnabled, thresholdDays, includeUntouched, minutesAlertEnabled, minutesThresholdDays, minutesThreshold ]
      properties:
        touchAlertEnabled:
          type: boolean
          description: '条件1（未接触アラート）をこの科目で有効にするか'
        thresholdDays:
          type: integer
          minimum: 1
          description: '条件1: 何日間学習がなければ警告するか'
        includeUntouched:
          type: boolean
          description: '条件1: 一度も学習していない場合も警告対象にするか'
        minutesAlertEnabled:
          type: boolean
          description: '条件2（学習時間不足アラート）をこの科目で有効にするか'
        minutesThresholdDays:
          type: integer
          minimum: 1
          description: '条件2: 直近何日間の学習時間を判定するか'
        minutesThreshold:
          type: integer
          minimum: 1
          description: '条件2: 何分未満なら警告するか'

    SubjectSettings:
      type: object
      properties:
        finalTarget: { type: string, nullable: true, maxLength: 500, description: '最終目標（科目全体）' }
        themeColor: { type: string, nullable: true, pattern: '^#[0-9a-fA-F]{6}$', description: 'テーマカラー（16進数 #rrggbb）。nullの場合は科目名ハッシュから自動生成' }

    SubjectMonthlyGoal:
      type: object
      required: [ year, month ]
      properties:
        year: { type: integer, description: '対象年' }
        month: { type: integer, minimum: 1, maximum: 12, description: '対象月' }
        goal: { type: string, nullable: true, maxLength: 1000, description: '今月の方針' }

    SubCategoryRank:
      type: string
      enum: [ A, B, C, D, E ]
      description: '小分類のランク（A が最重要）'

    SubCategory:
      type: object
      required: [ id, subject, name, rank, createdAt, updatedAt ]
      properties:
        id: { type: integer }
        subject: { $ref: '#/components/schemas/Subject' }
        name: { type: string }
        rank:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/SubCategoryRank'
          description: 'ランク（A〜E / 未設定は null）'
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SubCategoryInput:
      type: object
      required: [ subject, name ]
      properties:
        subject: { $ref: '#/components/schemas/Subject' }
        name: { type: string }
        rank:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/SubCategoryRank'
          description: 'ランク（A〜E / 未設定は null）'

    MonthlySettings:
      type: object
      required: [ year, month, targetMin, targetMax ]
      properties:
        year: { type: integer }
        month: { type: integer, minimum: 1, maximum: 12 }
        targetMin: { type: number, description: '月間目標下限（時間）' }
        targetMax: { type: number, description: '月間目標上限（時間）' }

    MonthlySettingsInput:
      type: object
      required: [ targetMin, targetMax ]
      properties:
        targetMin: { type: number, description: '月間目標下限（時間）' }
        targetMax: { type: number, description: '月間目標上限（時間）' }

    StudySession:
      type: object
      required: [ id, dailyLogDate, timeSlot, minutes, subject, material ]
      properties:
        id: { type: integer }
        dailyLogDate: { type: string, format: date }
        timeSlot: { $ref: '#/components/schemas/TimeSlot' }
        minutes: { type: integer, minimum: 1 }
        subject: { $ref: '#/components/schemas/Subject' }
        material: { $ref: '#/components/schemas/Material' }
        subCategory: { type: string, nullable: true, description: '小分類名（未登録なら自動登録）' }
        memo: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    PracticeSessionInput:
      type: object
      required: [ subject, date, totalElapsedMs, questions ]
      properties:
        subject: { $ref: '#/components/schemas/Subject' }
        date: { type: string, format: date }
        totalElapsedMs: { type: integer, minimum: 0, description: '演習全体の経過時間（ミリ秒）' }
        questions:
          type: array
          items:
            type: object
            required: [ index, judgement, elapsedMs ]
            properties:
              index: { type: integer, description: '問題番号（1始まり）' }
              judgement: { type: string, description: '回答（複数設問はカンマ区切り、例: "ア,イ"）' }
              elapsedMs: { type: integer, minimum: 0, description: 'この問題にかかった時間（ms）' }
              note: { type: string, nullable: true, description: '選択した選択肢のメモ（複数設問は改行結合）' }
              memos:
                type: array
                description: '設問ごとの全選択肢メモ（judgement の順に対応）'
                items:
                  type: object
                  additionalProperties: { type: string }
                  description: '選択肢をキー、メモ本文を値とするオブジェクト（例: {"ア": "〜", "イ": "〜"}）'

    PracticeAnswerRecord:
      type: object
      required: [ index, answers, elapsedMs ]
      properties:
        index: { type: integer, description: '問題番号（1始まり）' }
        answers:
          type: array
          items:
            type: object
            required: [ answer, isDoubtful ]
            properties:
              answer: { type: string, description: '選択肢（ア〜オ）または記述テキスト' }
              isDoubtful: { type: boolean }
              note: { type: string, nullable: true, description: '選択した選択肢のメモ' }
              memos:
                type: object
                additionalProperties: { type: string }
                description: '全選択肢のメモ（例: {"ア": "〜", "イ": "〜"}）'
        elapsedMs: { type: integer, minimum: 0, description: 'この問題にかかった時間（ms）' }

    PracticeSessionDraft:
      type: object
      required: [ subject, currentIndex, log ]
      properties:
        subject: { $ref: '#/components/schemas/Subject' }
        currentIndex: { type: integer, minimum: 1, description: '次に解く問題番号' }
        log:
          type: array
          items: { $ref: '#/components/schemas/PracticeAnswerRecord' }

    PracticeSessionResult:
      type: object
      required: [ id, subject, date, totalMinutes ]
      properties:
        id: { type: integer }
        subject: { $ref: '#/components/schemas/Subject' }
        date: { type: string, format: date }
        totalMinutes: { type: integer, description: '切り上げ後の分数（WorkspaceのStudySession作成に使用）' }

    StudySessionInput:
      type: object
      required: [ timeSlot, minutes, subject, material ]
      properties:
        timeSlot: { $ref: '#/components/schemas/TimeSlot' }
        minutes: { type: integer, minimum: 1 }
        subject: { $ref: '#/components/schemas/Subject' }
        material: { $ref: '#/components/schemas/Material' }
        subCategory: { type: string, nullable: true, description: '小分類名（未登録なら自動登録）' }
        memo: { type: string, nullable: true }

    DailyLog:
      type: object
      required: [ date, isCompleted, studySessions ]
      properties:
        date: { type: string, format: date }
        reflection: { type: string, nullable: true }
        isCompleted: { type: boolean }
        completedAt: { type: string, format: date-time, nullable: true }
        studySessions:
          type: array
          items: { $ref: '#/components/schemas/StudySession' }
        totalMinutes: { type: integer, readOnly: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    DailyLogSummary:
      type: object
      required: [ date, isCompleted, totalMinutes, sessionCount, slotMinutes, subjectMinutes ]
      properties:
        date: { type: string, format: date }
        isCompleted: { type: boolean }
        totalMinutes: { type: integer }
        sessionCount: { type: integer }
        reflection:
          type: string
          nullable: true
          description: '振り返りメモ'
        slotMinutes:
          type: object
          description: 時間帯スロット別の学習時間（分）
          required: [ morning, lunch, night ]
          properties:
            morning: { type: integer }
            lunch: { type: integer }
            night: { type: integer }
        subjectMinutes:
          type: object
          description: 科目別の学習時間（分）
          additionalProperties:
            type: integer

    Proficiency:
      type: string
      enum: [ '○', '△', '×' ]

    FailureType:
      type: string
      enum: [ '定義', '解法', 'ケアレス' ]

    Problem:
      type: object
      required: [ id, subject, material, questionRef, proficiency, failureTypes, isGoodQuestion, isFormula, solvedAt, createdAt, updatedAt ]
      properties:
        id: { type: integer }
        subject: { $ref: '#/components/schemas/Subject' }
        material: { $ref: '#/components/schemas/Material' }
        subCategory: { type: string, nullable: true, description: '小分類名（未登録なら自動登録）' }
        questionRef: { type: string }
        note: { type: string, nullable: true }
        proficiency: { $ref: '#/components/schemas/Proficiency' }
        failureTypes: { type: array, items: { $ref: '#/components/schemas/FailureType' } }
        isGoodQuestion: { type: boolean }
        isFormula: { type: boolean, description: '公式・定義フラグ。true の場合 /flash-card?formulaOnly=true の対象になる' }
        solvedAt: { type: string, format: date }
        lastTouchedAt: { type: string, format: date-time, nullable: true, description: '直近にレビュー（今日の5問で確認）した日時。PUT /problems/:id で自動更新される' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ProblemQuiz:
      type: object
      description: Problem に紐づく良問クイズ（FlashBugfix セッション中に「良問」登録されたクイズデータ）
      required: [ id, problemId, quizType, question, explanation, createdAt ]
      properties:
        id: { type: integer }
        problemId: { type: integer }
        quizType:
          type: string
          enum: [ multiple_choice, word_card ]
          description: 'multiple_choice: 選択式 / word_card: 単語カード（表裏のみ）'
        question: { type: string }
        options: { type: array, items: { type: string }, description: 'multiple_choice のみ。word_card の場合は空配列または省略' }
        correctIndex: { type: integer, nullable: true, description: '正解の選択肢インデックス（0始まり）。word_card の場合は null' }
        explanation: { type: string }
        createdAt: { type: string, format: date-time }

    ProblemQuizInput:
      type: object
      required: [ quizType, question, explanation ]
      description: |
        `quizType` によって必須フィールドが変わる。
        - `multiple_choice`: `options`（minItems: 2）と `correctIndex` が必須
        - `word_card`: `options` と `correctIndex` は不要（省略可）
      properties:
        quizType:
          type: string
          enum: [ multiple_choice, word_card ]
          description: 'multiple_choice: 選択式 / word_card: 単語カード'
        question: { type: string }
        options: { type: array, items: { type: string }, description: 'multiple_choice のみ必須（minItems: 2）' }
        correctIndex: { type: integer, nullable: true, description: '正解の選択肢インデックス（0始まり）。multiple_choice のみ必須' }
        explanation: { type: string }

    ProblemInput:
      type: object
      required: [ subject, questionRef, proficiency, failureTypes, isGoodQuestion, solvedAt ]
      description: |
        `materialId` と `materialName` のどちらか一方が必須。
        - `materialId` が指定された場合は既存の教材を使用する。
        - `materialId` が null で `materialName` が指定された場合は新規作成する。
      properties:
        subject: { $ref: '#/components/schemas/Subject' }
        materialId: { type: integer, nullable: true, description: '既存教材の ID（materialName と排他）' }
        materialName: { type: string,  nullable: true, maxLength: 255, description: '新規作成する教材名（materialId と排他）' }
        subCategory: { type: string, nullable: true, description: '小分類名（未登録なら自動登録）' }
        questionRef: { type: string }
        note: { type: string, nullable: true }
        proficiency: { $ref: '#/components/schemas/Proficiency' }
        failureTypes: { type: array, items: { $ref: '#/components/schemas/FailureType' } }
        isGoodQuestion: { type: boolean }
        isFormula: { type: boolean, description: '公式・定義フラグ（省略時は false）' }
        solvedAt: { type: string, format: date }

    DashboardStats:
      type: object
      required: [ thisWeekTotalMinutes, allTotalMinutes, thisMonthMinutes, thisMonthDays, last7DaysMinutes, weeklyAvgMinutes, subjectMinutes, allSubjectMinutes, lastTouchedBySubject, dailyMinutes ]
      properties:
        thisWeekTotalMinutes: { type: integer }
        currentStreak: { type: integer }
        allTotalMinutes: { type: integer }
        allTotalDays: { type: integer }
        thisMonthMinutes: { type: integer }
        thisMonthDays: { type: integer }
        last7DaysMinutes: { type: integer }
        weeklyAvgMinutes: { type: number, format: float }
        subjectMinutes:
          type: array
          items:
            type: object
            required: [ subject, minutes ]
            properties:
              subject: { type: string }
              minutes: { type: integer }
        allSubjectMinutes:
          type: array
          items:
            type: object
            required: [ subject, minutes ]
            properties:
              subject: { type: string }
              minutes: { type: integer }
        lastTouchedBySubject:
          type: array
          description: '全科目の最終学習日一覧。アラート判定には GET /api/subjects/alert-status を使用すること'
          items:
            type: object
            required: [ subject, lastDate ]
            properties:
              subject: { type: string }
              lastDate: { type: string, format: date, nullable: true }
        dailyMinutes:
          type: array
          items:
            type: object
            required: [ date, minutes ]
            properties:
              date: { type: string, format: date }
              minutes: { type: integer }

    SubjectActivityDay:
      type: object
      required: [ date, studyMinutes, problemCount ]
      properties:
        date: { type: string, format: date, description: '対象日（YYYY-MM-DD）' }
        studyMinutes: { type: integer, description: 'その日の該当科目の学習時間（分）' }
        problemCount: { type: integer, description: 'その日に追加された苦手問題の数（solvedAt 一致）' }

    SubjectSummaryItem:
      type: object
      description: 科目ごとの集計サマリ（GET /subjects/summary で使用）
      required: [ subject, studyMinutes, problemCount, failureStats ]
      properties:
        subject:
          type: string
        finalTarget:
          type: string
          nullable: true
          description: '最終目標（SubjectSettings.finalTarget）'
        monthlyGoal:
          type: string
          nullable: true
          description: '今月の方針（SubjectMonthlyGoal.goal — year/month に対応）'
        studyMinutes:
          type: integer
          description: '指定月の学習時間（分）'
        problemCount:
          type: integer
          description: '習熟度が △ または × の弱点問題数'
        failureStats:
          type: array
          description: 'ミスの種類別の件数と割合'
          items:
            type: object
            required: [ type, count, ratio ]
            properties:
              type: { $ref: '#/components/schemas/FailureType' }
              count: { type: integer }
              ratio: { type: number, format: float, minimum: 0, maximum: 1 }
        recentExamScore:
          type: object
          nullable: true
          description: '直近の完了済み過去問スコア。未実施なら null'
          required: [ examYear, score, rankStats ]
          properties:
            examYear: { type: integer, description: '過去問の年度' }
            score: { type: integer, description: '獲得点数' }
            completedAt: { type: string, format: date, nullable: true, description: '完了日' }
            rankStats:
              type: array
              description: 'ランク別正答率'
              items:
                type: object
                required: [ rank, correctRate, count ]
                properties:
                  rank: { type: string }
                  correctRate: { type: number, format: float, minimum: 0, maximum: 1 }
                  count: { type: integer }

    SubjectsSummary:
      type: object
      description: '全科目の月別サマリ（GET /subjects/summary のレスポンス）'
      required: [ year, month, subjects ]
      properties:
        year:
          type: integer
          description: '集計対象年'
        month:
          type: integer
          minimum: 1
          maximum: 12
          description: '集計対象月'
        subjects:
          type: array
          description: '登録済み科目ごとの詳細（studyMinutes=0 の科目も含む）'
          items:
            $ref: '#/components/schemas/SubjectSummaryItem'

    DashboardResponse:
      type: object
      description: 'ダッシュボード初期表示に必要なデータをまとめて返す（GET /dashboard のレスポンス）'
      required: [ stats, todayLog, prevDayLog, alertItems ]
      properties:
        stats:
          $ref: '#/components/schemas/DashboardStats'
        todayLog:
          allOf:
            - $ref: '#/components/schemas/DailyLog'
          nullable: true
          description: '今日の学習ログ（未作成なら null）'
        prevDayLog:
          allOf:
            - $ref: '#/components/schemas/DailyLog'
          nullable: true
          description: |
            前日の未完了ログ。サーバー現在時刻が 04:00 以降かつ前日ログが未完了・totalMinutes>0 の場合のみ返す。
            それ以外は null。
        alertItems:
          type: array
          items:
            $ref: '#/components/schemas/AlertStatusItem'

    MorningQuizItem:
      type: object
      required: [ id, subject, problem_context, quiz ]
      properties:
        id: { type: integer }
        subject: { type: string }
        sub_category: { type: string, nullable: true }
        problem_context:
          type: object
          properties:
            original_ref: { type: string, nullable: true }
            user_memo: { type: string, nullable: true }
            material_name: { type: string, nullable: true, description: '教材名' }
        quiz:
          type: object
          nullable: true
          properties:
            question: { type: string }
            options: { type: array, items: { type: string } }
            correct_index: { type: integer, minimum: 0, maximum: 3 }
            explanation: { type: string }
        last_touched_at: { type: string, format: date, nullable: true }

    TouchedOrder:
      type: string
      enum: [ recent, old ]
      description: 'recent: 最近触れた問題を優先 / old: 長く触れていない問題を優先'

    MorningBugfixSession:
      type: object
      required: [ session_id, meta, questions ]
      properties:
        session_id: { type: string, example: 'morning_bugfix_20260512' }
        meta:
          type: object
          properties:
            total_questions: { type: integer }
            strategy:
              type: string
              enum: [ definition_focused, flash_bugfix, word_card, formula_check ]
              description: 'definition_focused: Morning Bugfix / flash_bugfix: Flash Bugfix 一問一答 / word_card: 単語カード / formula_check: 公式チェック'
        questions:
          type: array
          items: { $ref: '#/components/schemas/MorningQuizItem' }

    Error:
      type: object
      required: [ message ]
      properties:
        message: { type: string }
        errors:
          type: object
          additionalProperties:
            type: array
            items: { type: string }

    # ── Exam ────────────────────────────────────────────────────────────────────

    Rank:
      type: string
      enum: [ A, B, C, D, E ]
      description: 問題の重要度ランク（A=最重要〜E=低優先）

    ExamSessionStatus:
      type: string
      enum: [ in_progress, scoring, completed ]
      description: 'in_progress: 解答中, scoring: 自己採点中, completed: 完了'

    ExamQuestion:
      type: object
      required: [ id, examSessionId, sortOrder, displayId, isSub, hasChildren, rank, isDoubtful, point ]
      properties:
        id: { type: integer }
        examSessionId: { type: integer }
        sortOrder: { type: integer, description: '表示順（0始まり）' }
        displayId: { type: string, description: '第1問・設問1 など表示用ID' }
        isSub: { type: boolean, description: '設問（小問）かどうか' }
        hasChildren: { type: boolean, description: '小問を持つ親問かどうか' }
        rank: { $ref: '#/components/schemas/Rank' }
        myAnswer: { type: string, nullable: true, description: 'ア〜オ または記述解答（未回答時はnull）' }
        answeredTimeMs: { type: integer, nullable: true, description: 'サーバー確定の回答時間（ミリ秒）' }
        answeredStartedAt: { type: string, format: date-time, nullable: true, description: '回答開始時刻（UTC ISO8601）' }
        answeredFinishedAt: { type: string, format: date-time, nullable: true, description: '回答確定時刻（UTC ISO8601）' }
        isCorrect: { type: boolean, nullable: true, description: '正誤（null=未採点）' }
        isDoubtful: { type: boolean, description: '要復習フラグ' }
        point: { type: integer, description: '配点' }
        note: { type: string, nullable: true, description: 'ミスメモ' }

    ExamQuestionInput:
      type: object
      required: [ sortOrder, displayId, isSub, hasChildren, rank, isDoubtful, point ]
      description: |
        試験完了時に送信する問題データ。
        `answeredStartedAt` と `answeredFinishedAt` を両方送ると、サーバーが `answeredTimeMs` を計算する。
        タイムスタンプがない場合は `answeredTimeMs` のフォールバック値を使用。
      properties:
        sortOrder: { type: integer }
        displayId: { type: string }
        isSub: { type: boolean }
        hasChildren: { type: boolean }
        rank: { $ref: '#/components/schemas/Rank' }
        myAnswer: { type: string, nullable: true }
        isCorrect: { type: boolean, nullable: true }
        isDoubtful: { type: boolean }
        point: { type: integer }
        note: { type: string, nullable: true }
        answeredTimeMs: { type: integer, nullable: true, description: 'フォールバック用（タイムスタンムがある場合はサーバーが上書き計算）' }
        answeredStartedAt: { type: string, format: date-time, nullable: true, description: '回答開始時刻（UTC ISO8601）' }
        answeredFinishedAt: { type: string, format: date-time, nullable: true, description: '回答確定時刻（UTC ISO8601）' }

    ExamSession:
      type: object
      required: [ id, subject, examYear, status, questions, totalScore, pureScore, correctCount, incorrectCount, doubtfulCount, createdAt ]
      properties:
        id: { type: integer }
        subject: { $ref: '#/components/schemas/Subject' }
        examYear: { type: string, example: 'R07', description: '受験年度（R07 等）' }
        status: { $ref: '#/components/schemas/ExamSessionStatus' }
        questions:
          type: array
          items: { $ref: '#/components/schemas/ExamQuestion' }
        totalScore: { type: integer, readOnly: true, description: '採点後の合計得点' }
        pureScore: { type: integer, readOnly: true, description: '要復習を除いた純粋な得点' }
        correctCount: { type: integer, readOnly: true }
        incorrectCount: { type: integer, readOnly: true }
        doubtfulCount: { type: integer, readOnly: true }
        createdAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }

    ExamSessionSummary:
      type: object
      required: [ id, subject, examYear, totalScore, pureScore, createdAt ]
      properties:
        id: { type: integer }
        subject: { $ref: '#/components/schemas/Subject' }
        examYear: { type: string }
        totalScore: { type: integer }
        pureScore: { type: integer }
        createdAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }

    ExamMistakeNote:
      type: object
      required: [ questionId, sessionId, examYear, displayId, rank, note, isDoubtful, completedAt ]
      properties:
        questionId: { type: integer }
        sessionId: { type: integer }
        examYear: { type: string }
        displayId: { type: string }
        rank: { $ref: '#/components/schemas/Rank' }
        note: { type: string }
        isDoubtful: { type: boolean }
        completedAt: { type: string, format: date-time }

    ExamRankStat:
      type: object
      required: [ rank, correctRate, count ]
      properties:
        rank: { $ref: '#/components/schemas/Rank' }
        correctRate: { type: number, format: float, minimum: 0, maximum: 1 }
        count: { type: integer, description: '該当ランクの問題数' }

    ExamSubjectStats:
      type: object
      required: [ subject, sessionCount, avgTotalScore, avgPureScore, rankStats, recentMistakes ]
      properties:
        subject: { $ref: '#/components/schemas/Subject' }
        sessionCount: { type: integer }
        avgTotalScore: { type: number, format: float }
        avgPureScore: { type: number, format: float }
        rankStats:
          type: array
          items: { $ref: '#/components/schemas/ExamRankStat' }
        recentMistakes:
          type: array
          items: { $ref: '#/components/schemas/ExamMistakeNote' }
          description: '直近10件のミスメモ（note が入力されているもの）'

    Flashcard:
      type: object
      required: [ id, front, back ]
      properties:
        id: { type: integer }
        lastTouchedAt: { type: string, format: date, nullable: true, description: '最終演習日（未演習はnull）' }
        subject: { $ref: '#/components/schemas/Subject' }
        front:
          type: object
          required: [ questionRef, solvedAt ]
          properties:
            questionRef: { type: string, description: '問題番号（例: 第12問）' }
            subCategory: { type: string, nullable: true, description: '小分類名' }
            material: { type: string, nullable: true, description: '教材名' }
            solvedAt: { type: string, format: date, description: '解答日' }
        back:
          type: object
          required: [ proficiency, failureTypes, isGoodQuestion ]
          properties:
            note: { type: string, nullable: true, description: '復習ノート（解法パターン・判断軸）' }
            proficiency: { $ref: '#/components/schemas/Proficiency' }
            failureTypes: { type: array, items: { $ref: '#/components/schemas/FailureType' } }
            isGoodQuestion: { type: boolean }
            isFormula: { type: boolean, description: '公式・定義フラグ（省略時は false）' }

    # ── Sprint ────────────────────────────────────────────────────────────────────

    SprintType:
      type: string
      enum: [ backlog, active ]
      description: |
        - `backlog`: バックログ（ユーザーごとに1件、削除・完了不可）
        - `active`: 通常スプリント

    SprintStatus:
      type: string
      enum: [ active, completed ]

    Sprint:
      type: object
      required: [ id, name, type, status, createdAt ]
      properties:
        id: { type: integer }
        name: { type: string }
        goal: { type: string, nullable: true, maxLength: 500, description: 'スプリントのテーマ・ゴール' }
        retrospective: { type: string, nullable: true, description: 'スプリント完了時の振り返り（Markdown）' }
        type: { $ref: '#/components/schemas/SprintType' }
        status: { $ref: '#/components/schemas/SprintStatus' }
        startDate: { type: string, format: date, nullable: true }
        endDate: { type: string, format: date, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    SprintInput:
      type: object
      required: [ name, startDate, endDate ]
      properties:
        name: { type: string, maxLength: 100, example: '第1回スプリント' }
        goal: { type: string, maxLength: 500, description: 'スプリントのテーマ・ゴール（任意）' }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }

    SprintUpdateInput:
      type: object
      properties:
        name: { type: string, maxLength: 100 }
        goal: { type: string, nullable: true, maxLength: 500 }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }

    SprintSubCategoryStats:
      type: object
      required: [ subCategoryId, subCategoryName, total, done ]
      properties:
        subCategoryId: { type: integer }
        subCategoryName: { type: string }
        total: { type: integer, description: 'スプリント内のチケット総数' }
        done: { type: integer, description: '完了済みチケット数' }

    SprintStats:
      type: object
      required: [ sprintId, sprintName, total, done, doing, todo, completionRate, bySubCategory ]
      properties:
        sprintId: { type: integer }
        sprintName: { type: string }
        total: { type: integer, description: 'チケット総数' }
        done: { type: integer, description: '完了数' }
        doing: { type: integer, description: '着手中数' }
        todo: { type: integer, description: '未着手数' }
        completionRate:
          type: number
          format: float
          minimum: 0
          maximum: 100
          description: '消化率（%）= done / total × 100'
        avgCompleteDays:
          type: number
          format: float
          nullable: true
          description: '作成から完了までの平均日数。完了チケットがない場合は null'
        bySubCategory:
          type: array
          items: { $ref: '#/components/schemas/SprintSubCategoryStats' }
          description: '小分類別の集計（1チケットが複数小分類を持つ場合は各小分類にカウント）'

    # ── StudyTicket ───────────────────────────────────────────────────────────────

    TicketStatus:
      type: string
      enum: [ todo, doing, done ]

    TicketPriority:
      type: string
      enum: [ high, medium, low ]

    TicketType:
      type: string
      enum: [ knowledge, practice, understanding, memorization ]
      description: |
        - `knowledge`: 知識補強型（整理・比較）
        - `practice`: 演習型（N問連続正解など）
        - `understanding`: 理解整理型（説明できるレベルまで）
        - `memorization`: 暗記型（用語・数字）

    TicketSource:
      type: string
      enum: [ wrong_answer, load_map, review, manual ]
      description: |
        - `wrong_answer`: 過去問ミスから発生
        - `load_map`: ロードマップ
        - `review`: 定期見直しから発生
        - `manual`: 手動作成

    TicketSubCategoryRef:
      type: object
      required: [ id, name ]
      properties:
        id: { type: integer }
        name: { type: string }
        subject: { type: string, nullable: true, description: '親科目名' }

    StudyTicket:
      type: object
      required: [ id, sprintId, subject, title, acceptanceCriteria, dueDate, status, priority, ticketType, source, subCategories, createdAt ]
      properties:
        id: { type: integer }
        sprintId: { type: integer }
        subject: { type: string, description: '紐づく科目名' }
        title: { type: string }
        acceptanceCriteria: { type: string, description: '達成条件（曖昧な表現禁止）' }
        dueDate: { type: string, format: date }
        status: { $ref: '#/components/schemas/TicketStatus' }
        priority: { $ref: '#/components/schemas/TicketPriority' }
        ticketType: { $ref: '#/components/schemas/TicketType' }
        source: { $ref: '#/components/schemas/TicketSource' }
        estimateMinutes: { type: integer, nullable: true, minimum: 1, maximum: 480 }
        subCategories:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/TicketSubCategoryRef' }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    StudyTicketInput:
      type: object
      required: [ subject, title, acceptanceCriteria, dueDate, priority, ticketType, source, subCategoryIds ]
      properties:
        sprintId:
          type: integer
          nullable: true
          description: '省略またはnullの場合はバックログに追加される'
        subject: { type: string, description: '紐づく科目名（必須）' }
        title: { type: string, maxLength: 255 }
        acceptanceCriteria: { type: string, description: '達成条件（具体的に記述）' }
        dueDate: { type: string, format: date }
        priority: { $ref: '#/components/schemas/TicketPriority' }
        ticketType: { $ref: '#/components/schemas/TicketType' }
        source: { $ref: '#/components/schemas/TicketSource' }
        estimateMinutes: { type: integer, nullable: true, minimum: 1, maximum: 480 }
        subCategoryIds:
          type: array
          minItems: 1
          items: { type: integer }
          description: '小分類IDの配列（1つ以上必須、選択した科目に属するものに限る）'

    StudyTicketUpdateInput:
      type: object
      properties:
        subject: { type: string, description: '紐づく科目名' }
        title: { type: string, maxLength: 255 }
        acceptanceCriteria: { type: string }
        dueDate: { type: string, format: date }
        status: { $ref: '#/components/schemas/TicketStatus' }
        priority: { $ref: '#/components/schemas/TicketPriority' }
        ticketType: { $ref: '#/components/schemas/TicketType' }
        source: { $ref: '#/components/schemas/TicketSource' }
        estimateMinutes: { type: integer, nullable: true, minimum: 1, maximum: 480 }
        subCategoryIds:
          type: array
          minItems: 1
          items: { type: integer }
          description: '送信した場合は全量置き換え（sync）'

    # ── TicketNote ───────────────────────────────────────────────────────────────

    TicketNote:
      type: object
      required: [ id, ticketId, body, createdAt, updatedAt ]
      properties:
        id: { type: integer }
        ticketId: { type: integer }
        body: { type: string, description: 'ノート本文（Markdown可）' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    TicketNoteInput:
      type: object
      required: [ body ]
      properties:
        body: { type: string, maxLength: 2000, description: 'ノート本文' }

    Snippet:
      type: object
      required: [ id, title, content ]
      properties:
        id: { type: integer }
        title: { type: string, maxLength: 60 }
        content: { type: string, maxLength: 2000 }

    SnippetInput:
      type: object
      required: [ title, content ]
      properties:
        title: { type: string, maxLength: 60 }
        content: { type: string, maxLength: 2000 }


paths:
  # ── Stopwatch ─────────────────────────────────────────────────────────────────

  /stopwatch:
    get:
      summary: ストップウォッチの現在状態を取得
      tags: [ Stopwatch ]
      description: |
        `is_running = true` の場合、`elapsed_seconds` には
        `保存済み累積秒数 + (現在時刻 - started_at)` を加算した値を返す。
        フロントはこの値をそのまま表示すればよい。
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StopwatchState' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /stopwatch/start:
    post:
      summary: ストップウォッチを開始
      tags: [ Stopwatch ]
      description: |
        `is_running = true` にして `started_at` をサーバー現在時刻で記録する。
        すでに計測中の場合は **409 Conflict** を返す。
        `elapsed_seconds` はリセットしない（累積）。
      responses:
        '200':
          description: 開始成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StopwatchState' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'

  /stopwatch/stop:
    post:
      summary: ストップウォッチを停止
      tags: [ Stopwatch ]
      description: |
        `elapsed_seconds += (現在時刻 - started_at)` を加算し、
        `is_running = false`、`started_at = null` にする。
        すでに停止中の場合は何もせず現在の状態を返す。
      responses:
        '200':
          description: 停止成功（または停止済みのまま）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StopwatchState' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /stopwatch/reset:
    post:
      summary: ストップウォッチをリセット
      tags: [ Stopwatch ]
      description: |
        `elapsed_seconds = 0`、`is_running = false`、`started_at = null` にリセットする。
        計測中・停止中いずれの状態でもリセット可能。
      responses:
        '200':
          description: リセット成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StopwatchState' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Subjects ─────────────────────────────────────────────────────────────────

  /subjects:
    get:
      summary: 科目一覧を取得
      tags: [ Subjects ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { type: string }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /subjects/{name}/settings:
    parameters:
      - name: name
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 科目設定を取得（最終目標など月によらない設定）
      tags: [ Subjects ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectSettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      summary: 科目設定を保存
      tags: [ Subjects ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SubjectSettings' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectSettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /subjects/summary:
    get:
      summary: 全科目の月別サマリを取得
      tags: [ Subjects ]
      description: |
        登録済み全科目について、指定月の学習時間・弱点問題数・ミス統計・直近過去問スコアを返す。
        `year`/`month` を省略するとサーバー現在年月が使用される。
        studyMinutes=0 の科目も含む。
      parameters:
        - name: year
          in: query
          required: false
          schema: { type: integer }
          description: '対象年（省略時: サーバー現在年）'
        - name: month
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 12 }
          description: '対象月（省略時: サーバー現在月）'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectsSummary' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /subjects/alert-status:
    get:
      summary: 全科目のアラート判定データを取得
      tags: [ Subjects ]
      description: |
        認証ユーザーの全科目について、アラート判定に必要なデータを一括返却する。
        各科目の `recentMinutes` は、その科目の `SubjectAlertSettings.minutesThresholdDays` を
        ウィンドウとして計算した学習時間（分）。
        アラート設定が未保存の科目はデフォルト設定（touchAlertEnabled=true, minutesAlertEnabled=false）を使用する。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AlertStatusItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /subjects/{name}/alert-settings:
    parameters:
      - name: name
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 科目別アラート設定を取得
      tags: [ Subjects ]
      description: 未設定の場合は touchAlertEnabled=true, minutesAlertEnabled=false のデフォルト値を返す。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectAlertSettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      summary: 科目別アラート設定を保存
      tags: [ Subjects ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SubjectAlertSettings' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectAlertSettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /subjects/{name}/monthly-goal/{year}/{month}:
    parameters:
      - name: name
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
      - name: year
        in: path
        required: true
        schema: { type: integer }
      - name: month
        in: path
        required: true
        schema: { type: integer, minimum: 1, maximum: 12 }
    get:
      summary: 科目の月別方針を取得
      tags: [ Subjects ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectMonthlyGoal' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      summary: 科目の月別方針を保存
      tags: [ Subjects ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                goal: { type: string, nullable: true, maxLength: 1000 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubjectMonthlyGoal' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /subjects/{name}:
    parameters:
      - name: name
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    put:
      summary: 科目名を変更
      description: |
        科目名を変更する。関連するすべてのデータ（学習記録・苦手問題・試験記録）のリレーションが保持される。
      tags: [ Subjects ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ newName ]
              properties:
                newName: { type: string, description: '新しい科目名' }
      responses:
        '200':
          description: 変更成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  name: { type: string }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 科目を削除（カスケード）
      description: |
        科目を削除する。この科目に紐づく学習記録・苦手問題・試験記録もすべて削除される。
      tags: [ Subjects ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /subjects/{subject}/activity:
    parameters:
      - name: subject
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 科目の日別アクティビティを取得
      tags: [ Subjects ]
      description: |
        指定月の全日付について、該当科目の学習時間と苦手問題追加数を返す。
        学習記録がない日は studyMinutes: 0、問題追加がない日は problemCount: 0 として返す。
      parameters:
        - name: year
          in: query
          required: true
          schema: { type: integer }
        - name: month
          in: query
          required: true
          schema: { type: integer, minimum: 1, maximum: 12 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/SubjectActivityDay' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /subjects/{subject}/review-problems:
    parameters:
      - name: subject
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 今解くべき5問を取得
      tags: [ Subjects ]
      description: |
        以下の優先順位で苦手問題を最大5件返す。
        1. `lastTouchedAt`（最終演習日）が古い順。未演習（null）を最優先。
        2. `failureTypes` に `定義` を含むものを優先。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Flashcard' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /flashcards:
    get:
      summary: フラッシュカード一覧を取得
      tags: [ Subjects ]
      description: |
        苦手問題をフラッシュカード形式で返す。`subject` を指定した場合はその科目のみ、
        省略した場合は全科目を返す。選択アルゴリズムは戦略パターンで差し替え可能。
        現在はランダム抽出。
      parameters:
        - name: subject
          in: query
          required: false
          schema: { type: string }
          description: 科目名（省略時は全科目）
        - name: count
          in: query
          required: false
          schema: { type: integer, minimum: 1, default: 10 }
          description: 取得件数（省略時は10件）
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Flashcard' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /subjects/{subject}/flashcards:
    parameters:
      - name: subject
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 科目別フラッシュカード一覧を取得
      tags: [ Subjects ]
      description: |
        指定科目の苦手問題をフラッシュカード形式で返す。
        - **表（front）**: 問題番号・小分類・教材・解答日
        - **裏（back）**: 復習ノート・習熟度・ミスタイプ・良問フラグ

        `solved_at` 降順・`created_at` 降順・`id` 降順でソート。
        カーソルページネーション（`after` に直前の末尾 ID を指定）に対応。
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1 }
          description: 取得件数（省略時は全件）
        - name: after
          in: query
          required: false
          schema: { type: integer }
          description: このIDより後のレコードを取得（カーソル）
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Flashcard' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── Materials ────────────────────────────────────────────────────────────────

  /materials:
    get:
      summary: 教材一覧を取得
      tags: [ Materials ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { type: string }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /materials/{name}:
    parameters:
      - name: name
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた教材名
    put:
      summary: 教材名を変更
      tags: [ Materials ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ newName ]
              properties:
                newName: { type: string, description: '新しい教材名' }
      responses:
        '200':
          description: 変更成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  name: { type: string }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 教材を削除
      description: |
        教材を削除する。この教材が紐づく学習記録・苦手問題の material フィールドは空文字列に更新される。
      tags: [ Materials ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── Sub Categories ──────────────────────────────────────────────────────────

  /sub-categories:
    get:
      summary: 小分類一覧を取得
      tags: [ Sub Categories ]
      parameters:
        - name: subject
          in: query
          required: false
          schema: { $ref: '#/components/schemas/Subject' }
          description: 科目でフィルタ（省略時は全件）
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/SubCategory' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      summary: 小分類を追加
      tags: [ Sub Categories ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SubCategoryInput' }
      responses:
        '201':
          description: 追加成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubCategory' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /sub-categories/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    put:
      summary: 小分類を更新
      tags: [ Sub Categories ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SubCategoryInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SubCategory' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 小分類を削除
      tags: [ Sub Categories ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── Monthly Settings ────────────────────────────────────────────────────────

  /monthly-settings/{year}/{month}:
    parameters:
      - name: year
        in: path
        required: true
        schema: { type: integer }
      - name: month
        in: path
        required: true
        schema: { type: integer, minimum: 1, maximum: 12 }
    get:
      summary: 月間設定を取得
      tags: [ Monthly Settings ]
      description: 指定した年月の目標設定を返す。未設定の場合はデフォルト値を返す。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MonthlySettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      summary: 月間設定を保存
      tags: [ Monthly Settings ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MonthlySettingsInput' }
      responses:
        '200':
          description: 保存成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MonthlySettings' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── Daily Logs ──────────────────────────────────────────────────────────────

  /daily-logs:
    get:
      summary: 月間ログ一覧を取得
      tags: [ Daily Logs ]
      parameters:
        - name: year
          in: query
          required: true
          schema: { type: integer }
        - name: month
          in: query
          required: true
          schema: { type: integer, minimum: 1, maximum: 12 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/DailyLogSummary' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    post:
      summary: デイリーログを作成
      tags: [ Daily Logs ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ date ]
              properties:
                date: { type: string, format: date }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DailyLog' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: 同日のログが既に存在する
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: '指定された日付のログは既に存在します。' }
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /daily-logs/recent:
    get:
      summary: 直近のログ一覧を取得（カーソルベースページネーション）
      tags: [ Daily Logs ]
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 30 }
          description: 取得件数
        - name: before
          in: query
          required: false
          schema: { type: string, format: date }
          description: このdateより前のログを取得（カーソル）
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/DailyLogSummary' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /daily-logs/{date}:
    parameters:
      - name: date
        in: path
        required: true
        schema: { type: string, format: date }
    get:
      summary: 特定日のログを取得
      tags: [ Daily Logs ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DailyLog' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: 指定日のログが未作成
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: '指定された日付のログが見つかりません。' }
    put:
      summary: 振り返りテキストを更新
      tags: [ Daily Logs ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reflection: { type: string, nullable: true }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DailyLog' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      summary: デイリーログを削除
      description: 指定日のデイリーログとそれに紐づく学習セッションをすべて削除する。
      tags: [ Daily Logs ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /daily-logs/{date}/complete:
    parameters:
      - name: date
        in: path
        required: true
        schema: { type: string, format: date }
    post:
      summary: デイリーログを完了にする
      tags: [ Daily Logs ]
      responses:
        '200':
          description: 完了成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DailyLog' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /daily-logs/{date}/uncomplete:
    parameters:
      - name: date
        in: path
        required: true
        schema: { type: string, format: date }
    post:
      summary: デイリーログの完了を取り消す
      tags: [ Daily Logs ]
      responses:
        '200':
          description: 取り消し成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DailyLog' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /study-sessions:
    post:
      summary: 学習ブロックを追加
      tags: [ Study Sessions ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/StudySessionInput'
                - type: object
                  required: [ dailyLogDate ]
                  properties:
                    dailyLogDate: { type: string, format: date }
      responses:
        '201':
          description: 追加成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudySession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: 指定日のデイリーログが見つからない
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: '指定された日付のログが見つかりません。' }
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /study-sessions/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    put:
      summary: 学習ブロックを更新
      tags: [ Study Sessions ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/StudySessionInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudySession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 学習ブロックを削除
      tags: [ Study Sessions ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /problems:
    get:
      summary: 苦手問題一覧を取得
      tags: [ Problems ]
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1 }
          description: 取得件数（省略時は全件）。`solved_at` 降順・`created_at` 降順で先頭から取得。
        - name: q
          in: query
          required: false
          schema: { type: string }
          description: フリーワード検索。問題文や解説文から部分一致で検索。
        - name: subjects
          in: query
          required: false
          style: form
          explode: false
          schema:
            type: array
            items:
              type: string
          description: >
            科目による絞り込み。カンマ区切りで複数指定した場合、
            指定したすべての科目に該当する問題を抽出（AND検索）。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Problem' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      summary: 苦手問題を追加
      tags: [ Problems ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProblemInput' }
      responses:
        '201':
          description: 追加成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /problems/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    get:
      summary: 苦手問題を1件取得
      tags: [ Problems ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      summary: 苦手問題を更新
      tags: [ Problems ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProblemInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 苦手問題を削除
      tags: [ Problems ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /problems/{id}/quizzes:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
        description: 親 Problem の ID
    get:
      summary: 良問クイズ一覧を取得
      tags: [ Problems ]
      description: |
        指定した Problem に紐づく良問クイズを `createdAt` 降順で返す。
        ProblemQuickModal でのクイズ一覧表示に使用する。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ProblemQuiz' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      summary: 良問クイズを登録
      tags: [ Problems ]
      description: |
        FlashBugfix セッション中に「良問」ボタンを押したときに呼び出す。
        そのクイズデータを親 Problem の子レコードとして保存する。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProblemQuizInput' }
      responses:
        '201':
          description: 登録成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProblemQuiz' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /problems/{id}/quizzes/{quizId}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
      - name: quizId
        in: path
        required: true
        schema: { type: integer }
    delete:
      summary: 良問クイズを削除
      tags: [ Problems ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /problems/{id}/touch:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    post:
      summary: 苦手問題の最終演習日を今日に更新
      tags: [ Problems ]
      description: |
        `lastTouchedAt` を今日の日付に更新する。
        科目詳細の「今解くべき5問」を演習したあとに呼び出す。
      responses:
        '200':
          description: 更新後のFlashcard
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Flashcard' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /dashboard:
    get:
      summary: ダッシュボード初期表示データを一括取得
      tags: [ Dashboard ]
      description: |
        ダッシュボードページの初期表示に必要なデータを1リクエストで返す。
        - `stats` — 全期間・当月・今週の集計（`DashboardStats`）
        - `todayLog` — 今日の学習ログ（未作成なら null）
        - `prevDayLog` — 前日の未完了ログ（サーバー時刻 04:00 以降かつ未完了・totalMinutes>0 の場合のみ、それ以外は null）
        - `alertItems` — 全科目のアラート判定データ
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DashboardResponse' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Exam Sessions ───────────────────────────────────────────────────────────

  /exam-sessions:
    get:
      summary: 試験セッション一覧を取得
      tags: [ Exam ]
      parameters:
        - name: status
          in: query
          required: false
          schema: { $ref: '#/components/schemas/ExamSessionStatus' }
          description: ステータスでフィルタ（省略時は全件）
        - name: subject
          in: query
          required: false
          schema: { $ref: '#/components/schemas/Subject' }
          description: 科目でフィルタ
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ExamSessionSummary' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      summary: 試験セッションを作成
      tags: [ Exam ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ subject, examYear ]
              properties:
                subject: { $ref: '#/components/schemas/Subject' }
                examYear: { type: string, example: 'R07' }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /exam-sessions/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    get:
      summary: 試験セッション詳細を取得
      tags: [ Exam ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      summary: 試験セッションのメタデータを更新
      tags: [ Exam ]
      description: subject・examYear の編集に使用。status の直接変更も可能（scoring への変更など）。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subject: { $ref: '#/components/schemas/Subject' }
                examYear: { type: string }
                status: { $ref: '#/components/schemas/ExamSessionStatus' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: 試験セッションを削除
      tags: [ Exam ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /exam-sessions/{id}/complete:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    post:
      summary: 試験を完了として保存
      tags: [ Exam ]
      description: |
        解答・採点データを一括登録し、ステータスを completed に変更する。
        既存の questions は全て置き換えられる。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ subject, examYear, questions ]
              properties:
                subject: { $ref: '#/components/schemas/Subject' }
                examYear: { type: string }
                questions:
                  type: array
                  items: { $ref: '#/components/schemas/ExamQuestionInput' }
      responses:
        '200':
          description: 保存・完了成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /exam-sessions/quick-score:
    post:
      summary: スコアのみ記録（解答明細なし）
      tags: [ Exam ]
      description: |
        解答明細を持たず、合計得点・純粋得点だけを記録して completed セッションを作成する。
        過去に受験した試験を後から記録する場合などに使用する。
        pureScore を省略した場合は totalScore と同値として扱う。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ subject, examYear, totalScore ]
              properties:
                subject: { $ref: '#/components/schemas/Subject' }
                examYear: { type: string, example: 'R07' }
                totalScore: { type: integer, minimum: 0, maximum: 100 }
                pureScore:
                  type: integer
                  minimum: 0
                  maximum: 100
                  description: '省略時は totalScore と同値'
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSessionSummary' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /exam-sessions/{id}/questions/{sortOrder}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
        description: 試験セッション ID
      - name: sortOrder
        in: path
        required: true
        schema: { type: integer, minimum: 0 }
        description: 問題の表示順（0始まり）
    patch:
      summary: 問題の回答タイムスタンプを更新（中間記録）
      tags: [ Exam ]
      description: |
        問題の表示開始（`event: "start"`）または回答確定（`event: "finish"`）を記録する。

        **タイムスタンプはサーバー基準で記録する。** フロントが時刻を送る必要はない。

        | event | 動作 |
        |---|---|
        | `start` | `answered_started_at = now()` をセット（初回のみ。既に記録済みなら無視）|
        | `finish` | `answered_finished_at = now()` をセットし、`answered_time_ms` をサーバー計算 |

        問題レコードがまだ存在しない場合は、提供されたメタデータで新規作成する。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ event ]
              properties:
                event:
                  type: string
                  enum: [ start, finish ]
                  description: '`start` = 問題表示、`finish` = 回答確定'
                displayId:
                  type: string
                  description: 問題の表示 ID（初回のみ使用）
                isSub: { type: boolean }
                hasChildren: { type: boolean }
                rank: { $ref: '#/components/schemas/Rank' }
                isDoubtful: { type: boolean }
                point: { type: integer, minimum: 0 }
            examples:
              start:
                summary: 問題表示時
                value: { event: start, displayId: "第1問", isSub: false, hasChildren: false, rank: A, isDoubtful: false, point: 5 }
              finish:
                summary: 回答確定時
                value: { event: finish }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamQuestion' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── Exam Subject Stats ───────────────────────────────────────────────────────

  /exam-subjects/{subject}/stats:
    parameters:
      - name: subject
        in: path
        required: true
        schema: { $ref: '#/components/schemas/Subject' }
        description: URLエンコードされた科目名
    get:
      summary: 科目別の分析データを取得
      tags: [ Exam ]
      description: |
        指定科目のランク別正答率と直近のミスメモ（note があるもの）を返す。
        year/month を指定するとその月に completedAt がある試験セッションだけを集計する。
      parameters:
        - name: year
          in: query
          required: false
          schema: { type: integer }
        - name: month
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 12 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ExamSubjectStats' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /auth/google:
    post:
      summary: Googleログイン（Firebase ID Token検証）
      tags: [ Auth ]
      security: [ ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ id_token ]
              properties:
                id_token: { type: string, description: 'Firebase ID Token' }
      responses:
        '200':
          description: ログイン成功
          content:
            application/json:
              schema:
                type: object
                required: [ token, user ]
                properties:
                  token: { type: string, description: 'アクセストークン（Bearer）' }
                  user:
                    type: object
                    required: [ id, name, email ]
                    properties:
                      id: { type: integer }
                      name: { type: string }
                      email: { type: string, format: email }
        '401':
          description: IDトークンが無効
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: 'Firebase IDトークンが無効です。' }

  /auth/refresh:
    post:
      summary: トークンリフレッシュ
      tags: [ Auth ]
      security: [ ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ refresh_token ]
              properties:
                refresh_token: { type: string, description: 'Firebase リフレッシュトークン' }
      responses:
        '200':
          description: リフレッシュ成功
          content:
            application/json:
              schema:
                type: object
                required: [ token ]
                properties:
                  token: { type: string, description: '新しいアクセストークン（Bearer）' }
        '401':
          description: リフレッシュトークンが無効または期限切れ
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: 'リフレッシュトークンが無効です。再ログインしてください。' }

  /auth/me:
    get:
      summary: 認証済みユーザー情報取得
      tags: [ Auth ]
      security:
        - bearerAuth: [ ]
      responses:
        '200':
          description: ユーザー情報
          content:
            application/json:
              schema:
                type: object
                required: [ id, name, email ]
                properties:
                  id: { type: integer }
                  name: { type: string }
                  email: { type: string, format: email }
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Profile ──────────────────────────────────────────────────────────────────

  /profile:
    get:
      summary: プロフィール取得
      tags: [ Profile ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserProfile' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      summary: プロフィール更新（upsert）
      tags: [ Profile ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UserProfileInput' }
            example:
              nickname: やっぴー
              occupation: ITエンジニア（SE）
              goal: 中小企業診断士試験に一発合格（週45時間学習）
              weakAreas: 経営法務、中小企業経営・政策
              strongAreas: 財務・会計、経営情報システム
              interests: 物理学、歴史、哲学、言語学
              geminiToken: AIzaSy...
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserProfile' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /gemini/settings:
    get:
      summary: Gemini設定を取得
      tags: [ AI ]
      description: ユーザーが設定した使用モデル名を返す。未設定の場合は `geminiModel` が `null`。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [ geminiModel ]
                properties:
                  geminiModel:
                    type: string
                    nullable: true
                    description: '使用モデル名（例: gemini-2.5-flash, gemini-2.5-pro）'
              example:
                geminiModel: 'gemini-2.5-flash'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      summary: Gemini設定を更新
      tags: [ AI ]
      description: |
        使用モデル名を保存する。`ai_user_profiles` に upsert される。
        設定後の Gemini 呼び出し（AIアドバイス・画像解析・朝の復習）すべてにこのモデルが使用される。
        環境変数 `GEMINI_MODEL` がデフォルトとして使用され、この設定で上書きされる。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ geminiModel ]
              properties:
                geminiModel:
                  type: string
                  maxLength: 100
                  description: '使用モデル名'
            example:
              geminiModel: 'gemini-2.5-pro'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [ geminiModel ]
                properties:
                  geminiModel:
                    type: string
                    nullable: true
                    description: '保存されたモデル名'
              example:
                geminiModel: 'gemini-2.5-pro'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── Morning Bugfix ───────────────────────────────────────────────────────────

  /deg-bugfix:
    get:
      summary: DegBugfix セッションを取得
      tags: [ AI ]
      description: |
        ユーザーが「良問」登録した `ProblemQuiz` を元にクイズセッションを組み立てて返す。
        科目を指定した場合はその科目の良問のみを対象にする。省略時は全科目が対象。
        登録済み良問がない、または指定条件に合致する良問がない場合は 422 を返す。
      parameters:
        - name: subject
          in: query
          required: false
          schema: { type: string }
          description: '科目名フィルタ。省略時は全科目対象'
          example: '企業経営理論'
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 5, minimum: 1, maximum: 20 }
          description: '取得問題数。良問の総数が limit 未満の場合は全件返す'
          example: 5
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MorningBugfixSession' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: 条件に合致する良問クイズが存在しない
          content:
            application/json:
              schema:
                type: object
                required: [ message ]
                properties:
                  message: { type: string, example: '良問クイズが登録されていません' }

  /morning-bugfix:
    get:
      summary: 定義復習クイズセッションを取得（Morning Bugfix / Flash Bugfix）
      tags: [ AI ]
      description: |
        苦手問題から定義理解クイズを生成して返す汎用エンドポイント。
        新パラメータ（`failureType`, `subCategoryIds`, `touchedOrder`, `proficiency`, `limit`）を
        **1つも指定しない**場合は Morning Bugfix モードとして動作し、既存の挙動を維持する。
        いずれか1つでも指定した場合は Flash Bugfix モード（柔軟フィルタ）として動作する。

        ### Morning Bugfix モード（パラメータ未指定）
        デフォルト: `failureType=['定義']`, `limit=5`

        **3段階フォールバック:**
        1. `last_touched_at == 昨日` かつ `定義`（RANDOM）
        2. 直近7日以内の定義（昨日を除く）で補填（RANDOM）
        3. 全期間の定義を習熟度低い順（×→△→○）で補填

        `date` パラメータを指定するとその日を「今日」とみなす（テスト用）。

        ### Flash Bugfix モード（任意パラメータ指定）
        指定した条件で問題を抽出し、不足時は段階的に条件を緩めて補填する。
        複数選択されたパラメータ（`failureType`, `subCategoryIds`, `proficiency`）は、
        それぞれの配列内での **OR条件** としてフィルタリングされる。

        **フォールバック順:**
        1. `failureType` + `subCategoryIds`（フル条件）
        2. `failureType` のみ（`subCategoryIds` を緩める）
        3. `subCategoryIds` のみ（`failureType` を緩める）
        4. 全問題（習熟度低い順）
      parameters:
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: '基準日（Morning Bugfix モード専用・省略時: サーバー今日）'
        - name: failureType
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { $ref: '#/components/schemas/FailureType' }
          description: 'ミス種別フィルタ（複数選択可）。未指定なら全ミス種別対象（Morning Bugfix デフォルト: [定義]）'
          example: [ '定義', '解法' ]
        - name: subCategoryIds
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { type: integer }
          description: '小分類IDフィルタ（複数選択可）。未指定なら全小分類対象'
          example: [ 12, 15 ]
        - name: touchedOrder
          in: query
          required: false
          schema: { $ref: '#/components/schemas/TouchedOrder' }
          description: '最後に触れた日からの近さ。未指定なら RANDOM'
          example: 'old'
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 5, minimum: 1, maximum: 10 }
          description: '取得問題数（max: 10）'
          example: 10
        - name: proficiency
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { $ref: '#/components/schemas/Proficiency' }
          description: '習熟度フィルタ（複数選択可）。未指定なら △/× のみ'
          example: [ '△', '×' ]
        - name: subject
          in: query
          required: false
          schema: { type: string }
          description: '科目名フィルタ（Flash Bugfix 専用）。指定するとその科目の問題のみを対象にする'
          example: '企業経営理論'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MorningBugfixSession' }
              examples:
                morning_bugfix:
                  summary: Morning Bugfix（パラメータ未指定）
                  value:
                    session_id: 'morning_bugfix_20260512'
                    meta: { total_questions: 3, strategy: 'definition_focused' }
                    questions:
                      - id: 42
                        subject: '企業経営理論'
                        sub_category: '経営戦略論'
                        problem_context: { original_ref: '2023年 第12問', user_memo: 'PPM の定義' }
                        quiz:
                          question: '次のうちPPMにおける「花形」の定義として正しいものはどれか？'
                          options: [ '高シェア・高成長', '低シェア・高成長', '高シェア・低成長', '低シェア・低成長' ]
                          correct_index: 0
                          explanation: '花形は市場成長率・市場シェアともに高い事業...'
                        last_touched_at: '2026-05-11'
                flash_bugfix:
                  summary: Flash Bugfix（subjectPage から）
                  value:
                    session_id: 'flash_bugfix_20260512'
                    meta: { total_questions: 5, strategy: 'flash_bugfix' }
                    questions: [ ]
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/GeminiQuotaExceeded'
        '503':
          $ref: '#/components/responses/GeminiUnavailable'

  # ── Flash Card ───────────────────────────────────────────────────────────────

  /flash-card:
    get:
      summary: 単語カードセッションを取得（Flash Bugfix 単語カードモード）
      tags: [ AI ]
      description: |
        苦手問題から単語カード形式のセッションを生成して返す。
        レスポンス構造は `/morning-bugfix` と同じ `MorningBugfixSession` を使用する。
        フロントエンドでは `quiz.question` を表面、`quiz.explanation` を裏面として表示する。

        ### 公式チェックモード（`formulaOnly=true`）
        財務会計・経済学・運営管理などで重宝する公式・定義のみに絞った問題を返す。
        バックエンドは `is_formula=true` フラグを持つ問題のみを対象にフィルタリングする。

        ### フォールバック順（Flash Bugfix と同様）
        1. `failureType` + `subCategoryIds`（フル条件）
        2. `failureType` のみ
        3. `subCategoryIds` のみ
        4. 全問題（習熟度低い順）
      parameters:
        - name: subject
          in: query
          required: true
          schema: { type: string }
          description: 科目名
          example: '財務・会計'
        - name: failureType
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { $ref: '#/components/schemas/FailureType' }
          description: 'ミス種別フィルタ（複数選択可）。未指定なら全ミス種別対象'
          example: [ '定義', '解法' ]
        - name: subCategoryIds
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { type: integer }
          description: '小分類IDフィルタ（複数選択可）。未指定なら全小分類対象'
          example: [ 12, 15 ]
        - name: touchedOrder
          in: query
          required: false
          schema: { $ref: '#/components/schemas/TouchedOrder' }
          description: '最後に触れた日からの近さ。未指定なら RANDOM'
          example: 'old'
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 5, minimum: 1, maximum: 10 }
          description: '取得問題数（max: 10）'
          example: 5
        - name: proficiency
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items: { $ref: '#/components/schemas/Proficiency' }
          description: '習熟度フィルタ（複数選択可）。未指定なら △/× のみ'
          example: [ '△', '×' ]
        - name: formulaOnly
          in: query
          required: false
          schema: { type: boolean, default: false }
          description: |
            `true` にすると公式・定義フラグ（`is_formula=true`）が付いた問題のみを対象にする。
            財務会計・経済学・運営管理の公式チェックモードで使用する。
          example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MorningBugfixSession' }
              examples:
                word_card:
                  summary: 単語カードモード（formulaOnly=false）
                  value:
                    session_id: 'flash_card_20260516'
                    meta: { total_questions: 5, strategy: 'word_card' }
                    questions:
                      - id: 101
                        subject: '財務・会計'
                        sub_category: 'CVP分析'
                        problem_context: { original_ref: 'R04 第8問', user_memo: '' }
                        quiz:
                          question: '損益分岐点売上高の計算式を答えよ。'
                          options: [ ]
                          correct_index: 0
                          explanation: '固定費 ÷ 限界利益率。限界利益率 = (売上高 - 変動費) / 売上高'
                        last_touched_at: '2026-05-10'
                formula_check:
                  summary: 公式チェックモード（formulaOnly=true）
                  value:
                    session_id: 'flash_card_formula_20260516'
                    meta: { total_questions: 5, strategy: 'formula_check' }
                    questions: [ ]
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── AI Advice ────────────────────────────────────────────────────────────────

  /ai/advice:
    post:
      summary: AIアドバイスを生成
      tags: [ AI ]
      description: |
        ユーザーの学習ログ・苦手問題・目標データをもとに Gemini API を呼び出し、
        モード別のアドバイスを生成して返す。

        | モード | 内容 |
        |---|---|
        | `ANALYSIS` | 今週の達成率・残り時間を算出し、時間ブロック配置案を提案 |
        | `INSPIRATION` | 客観的な事実で学習努力を称える |
        | `ANALOGY` | 試験概念を趣味に例えて解説 |
        | `WARNING` | 危機感を煽る冷徹な鬼コーチ |
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ mode ]
              properties:
                mode:
                  $ref: '#/components/schemas/AiAdviceMode'
            examples:
              analysis:
                summary: ANALYSISモード
                value: { mode: ANALYSIS }
              inspiration:
                summary: INSPIRATIONモード
                value: { mode: INSPIRATION }
      responses:
        '200':
          description: アドバイス生成成功
          content:
            application/json:
              schema:
                type: object
                required: [ advice ]
                properties:
                  advice:
                    type: string
                    description: 生成されたアドバイス（100〜250文字程度）
                    example: '今週は財務・会計に集中しているが、法務が0分。残り12時間のうち3時間を法務に充て、過去問2年分を解くことで弱点を一気に潰せる。'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: modeが不正
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: 'The given data was invalid.', errors: { mode: [ 'modeは ANALYSIS, INSPIRATION, ANALOGY, WARNING のいずれかを指定してください。' ] } }
        '429':
          $ref: '#/components/responses/GeminiQuotaExceeded'
        '503':
          $ref: '#/components/responses/GeminiUnavailable'

  /ai/analysis:
    post:
      summary: 問題画像を解析して苦手問題に登録
      tags: [ AI ]
      description: |
        アップロードされた試験問題の画像を Gemini 1.5 Flash でマルチモーダル解析し、
        苦手問題 (Problem)のメタ情報を補足要素として解説画像を読み取り、noteに保存する。
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [ image, problemId ]
              properties:
                image:
                  type: string
                  format: binary
                  description: 問題画像（JPEG / PNG / WebP / HEIC、最大 10MB）
                memo:
                  type: string
                  nullable: true
                  maxLength: 2000
                  description: 解答時のユーザーメモ。指定するとGeminiのノート生成に間違えた原因・改善ポイントが追加される。
      responses:
        '201':
          description: 解析成功・苦手問題として保存
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          description: バリデーションエラー（ファイル形式・サイズ不正など）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: 'The given data was invalid.', errors: { image: [ '画像ファイルは必須です。' ] } }
        '429':
          $ref: '#/components/responses/GeminiQuotaExceeded'
        '500':
          description: Gemini が期待する JSON を返さなかった場合など
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: 'Gemini returned invalid JSON' }
        '503':
          $ref: '#/components/responses/GeminiUnavailable'

  # ── Practice sessions ────────────────────────────────────────────────────────

  /practice/sessions/draft:
    put:
      summary: 演習ドラフトを保存（途中再開用）
      tags: [ Practice ]
      description: |
        演習途中の状態を保存する。同一科目のドラフトが存在する場合は上書きする。
        フロントエンドは回答のたびに呼び出すことで、アプリ終了後の再開を可能にする。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PracticeSessionDraft' }
      responses:
        '204':
          description: 保存成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /practice/sessions/draft/{subject}:
    parameters:
      - name: subject
        in: path
        required: true
        schema: { type: string }
        description: URLエンコードされた科目名
    get:
      summary: 演習ドラフトを取得
      tags: [ Practice ]
      responses:
        '200':
          description: ドラフトが存在する
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PracticeSessionDraft' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ドラフトなし（演習未開始または完了・削除済み）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              example: { message: '指定された科目のドラフトが見つかりません。' }
    delete:
      summary: 演習ドラフトを削除（完了・破棄時）
      tags: [ Practice ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'

  /practice/sessions:
    post:
      summary: 演習セッションを保存する
      tags: [ Practice ]
      description: |
        一問一答演習の結果を保存する。
        バックエンドは受け取った totalElapsedMs を分単位（切り上げ）に変換し、
        該当日の DailyLog に StudySession を自動生成することを推奨する。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PracticeSessionInput' }
      responses:
        '201':
          description: 保存成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PracticeSessionResult' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── Sprint ────────────────────────────────────────────────────────────────────

  /sprints:
    get:
      summary: スプリント一覧を取得する
      tags: [ Sprint ]
      description: |
        ユーザーの全スプリントを返す。
        バックログスプリントが未作成の場合は自動生成される。
        レスポンスはバックログを先頭に、以降は作成日降順で返す。
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Sprint' }
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      summary: スプリントを作成する
      tags: [ Sprint ]
      description: type は常に `active`。バックログはシステムが自動生成するため手動作成不可。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SprintInput' }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Sprint' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /sprints/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }

    get:
      summary: スプリント詳細を取得する
      tags: [ Sprint ]
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Sprint' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      summary: スプリントを更新する
      tags: [ Sprint ]
      description: |
        バックログスプリントは `name` のみ変更可能。
        `startDate` / `endDate` はバックログに対して送信しても無視される。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SprintUpdateInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Sprint' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

    delete:
      summary: スプリントを削除する
      tags: [ Sprint ]
      description: バックログスプリントは削除不可（422 を返す）。
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /sprints/{id}/complete:
    post:
      summary: スプリントを完了する
      tags: [ Sprint ]
      description: |
        `status` を `completed` に変更し `completedAt` をセットする。
        バックログおよびすでに完了済みのスプリントには 422 を返す。
        振り返り（`retrospective`）は必須。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ retrospective ]
              properties:
                retrospective: { type: string, description: '振り返り（Markdown）' }
      responses:
        '200':
          description: 完了成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Sprint' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /sprints/{id}/stats:
    get:
      summary: スプリントのKPIを取得する
      tags: [ Sprint ]
      description: |
        チケット消化率・平均完了日数・小分類別集計を返す。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SprintStats' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ── StudyTicket ───────────────────────────────────────────────────────────────

  /tickets:
    get:
      summary: チケット一覧を取得する
      tags: [ StudyTicket ]
      description: |
        クエリパラメータで絞り込み可能。
        `sprintId` を省略した場合は全スプリントのチケットを返す。
        ソート順：`dueDate` 昇順 → `priority`（high → medium → low）降順。
      parameters:
        - name: sprintId
          in: query
          schema: { type: integer }
          description: 'スプリントIDで絞り込み（省略時は全件）'
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/TicketStatus' }
          description: 'ステータスで絞り込み'
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      summary: チケットを作成する
      tags: [ StudyTicket ]
      description: |
        `sprintId` を省略またはnullにした場合、バックログスプリントに自動配置される。
        `subCategoryIds` は1件以上必須。
        作成時の `status` は常に `todo`。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/StudyTicketInput' }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /tickets/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }

    get:
      summary: チケット詳細を取得する
      tags: [ StudyTicket ]
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      summary: チケットを更新する
      tags: [ StudyTicket ]
      description: |
        `subCategoryIds` を送信した場合は全量置き換え（sync）。
        `status` を `done` に変更すると `completedAt` が自動セットされる。
        `status` を `done` 以外に変更すると `completedAt` がクリアされる（再オープン相当）。
        専用エンドポイント（`/complete` / `/reopen`）の使用を推奨。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/StudyTicketUpdateInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

    delete:
      summary: チケットを削除する
      tags: [ StudyTicket ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /tickets/{id}/complete:
    post:
      summary: チケットを完了する
      tags: [ StudyTicket ]
      description: |
        `status` を `done` に変更し `completedAt` を自動セットする。
        すでに `done` の場合は 422 を返す。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: 完了成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /tickets/{id}/reopen:
    post:
      summary: チケットを再オープンする
      tags: [ StudyTicket ]
      description: |
        `status` を `todo` に戻し `completedAt` をクリアする。
        `done` 以外のチケットに対しては 422 を返す。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: 再オープン成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /tickets/{id}/sprint:
    patch:
      summary: チケットを別のスプリントに移動する
      tags: [ StudyTicket ]
      description: バックログを含む任意のスプリントへ移動できる。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ sprintId ]
              properties:
                sprintId: { type: integer, description: '移動先スプリントID' }
      responses:
        '200':
          description: 移動成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/StudyTicket' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ── TicketNote ────────────────────────────────────────────────────────────────

  /tickets/{id}/notes:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
        description: チケットID

    get:
      summary: チケットのノート一覧を取得する
      tags: [ StudyTicket ]
      description: 作成日昇順（スレッド順）で返す。
      responses:
        '200':
          description: 取得成功
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/TicketNote' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    post:
      summary: チケットにノートを追加する
      tags: [ StudyTicket ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TicketNoteInput' }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TicketNote' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /tickets/{id}/notes/{noteId}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
        description: チケットID
      - name: noteId
        in: path
        required: true
        schema: { type: integer }
        description: ノートID

    put:
      summary: ノートを更新する
      tags: [ StudyTicket ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TicketNoteInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TicketNote' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

    delete:
      summary: ノートを削除する
      tags: [ StudyTicket ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /auth/logout:
    post:
      summary: ログアウト
      tags: [ Auth ]
      description: |
        現在使用中のアクセストークンを削除し、セッションを終了します。
        内部的には Laravel Sanctum の `currentAccessToken()->delete()` を実行します。
      security:
        - bearerAuth: [ ]
      responses:
        '200':
          description: ログアウト成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: ログアウトしました。
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Snippets ─────────────────────────────────────────────────────────────────

  /snippets:
    get:
      summary: スニペット一覧を取得
      tags: [ Snippets ]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Snippet' }
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      summary: スニペットを作成
      tags: [ Snippets ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SnippetInput' }
      responses:
        '201':
          description: 作成成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Snippet' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /snippets/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer }
    put:
      summary: スニペットを更新
      tags: [ Snippets ]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SnippetInput' }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Snippet' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    delete:
      summary: スニペットを削除
      tags: [ Snippets ]
      responses:
        '204':
          description: 削除成功
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
